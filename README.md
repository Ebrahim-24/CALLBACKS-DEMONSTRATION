# CALLBACKS-DEMONSTRATION
this code demonstrates the implementation of a second activity in an Android application and showcases the usage of activity lifecycle callbacks (onStart(), onResume(), onPause(), onStop(), and onDestroy()). The toast messages provide feedback to the user about the current state of the activity.
